# Python
__pycache__/
*.py[cod]
venv/
.venv/

# macOS
.DS_Store

# Large artifacts / PII — excluded from the repo.
# wandb logs embed email/host/local paths; models/ is ~1GB; expert demos are large.
# Only selected final checkpoints are force-added (git add -f); see README.
wandb/
models/
data/*.npz
outputs/logs/

# Claude Code: keep personal local overrides out of the repo (.claude/settings.json is tracked).
.claude/settings.local.json

# Tracked (portfolio showcase): outputs/videos, outputs/images, outputs/evaluation_results.json
